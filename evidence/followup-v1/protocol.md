# Bounded follow-up v1 — frozen before lesson revision/evaluation

Approved scope: diagnose ordinary-word versus random-identifier transfer and improve lesson construction using acquisition-only validation. No adapter training or hyperparameter/prompt search beyond the explicit lesson revision limit. Preserve acquisition-v1 and its publication as the initial experiment, not the final broad answer.

Use the exact native model, twelve acquisition demonstrations, common schema, original supplied-rule diagnostic, and saved final adapter from acquisition-v1. Check model file hashes and saved adapter's file/parameter hashes. Verify initial adapter matches the original initialization; verify base invariance, exact restored logits, and replay four original adapter calls (indices 0,16,32,48) as a serialization diagnostic, reported separately.

Lesson construction: start with the unchanged acquisition-v1 generated lesson (candidate 0). Validate by prompting the base model with candidate+schema for each of the twelve acquisition inputs, exact-call score, no retries. Stop on 12/12. Otherwise generate a revised <=100-word lesson with the same base model, greedy max 256 tokens, from only the twelve checked examples, prior candidate, and all acquisition validation outputs/expected calls. At most two revisions, three candidates total. Stop at first 12/12; if none pass, retain highest acquisition-score candidate, earliest tie, and label it unvalidated. Do not hand-edit, supply the researcher's correct rule to the writer, or expose new evaluation inputs during lesson selection. Count original lesson generation plus every validation and revision call/time/token. Passing on reused acquisition inputs establishes construction consistency, not generalization. The model may retain examples in its generated lesson; disclose verbatim text.

After lesson selection, evaluate five branches: no acquisition, all twelve examples, selected lesson, supplied original rule, unchanged adapter. Fresh single-turn generation caches, greedy max 96 tokens, one pass, rotate branch order per input. No evaluation retries, no subsequent selection or training. Eight fresh ordinary words: lamp, boat (length 4), copper, garden (6), notebook, mountain (8), helicopter, strawberry (10). Generate eight random lowercase identifiers, two per length, Python Random(20260912), excluding all previous acquisition/development/evaluation strings and ordinary follow-up words. Check disjointness. These are paired by length but are not matched in tokenizer length or frequency; the lexical-stratum comparison is descriptive, not a causal isolation of lexicality.

Cross each of the sixteen strings with all four mode/priority combinations: 64 inputs and 320 evaluation calls. Report exact success by lexical stratum and seen (24 per stratum) versus withheld amber/urgent combination (8 per stratum), plus lengths and diagnostic fields. Sixteen identifiers are the diversity units, not 64 independent observations. Report raw counts without significance claims. Keep prior random evaluation separate; this is a fresh follow-up test, not a pooled retest selected for success.

Resources: same local MLX runtime, 20-minute cooperative total budget and 24 GB MLX allocation ceiling; no new model downloads. At most 320 evaluation + 36 validation + 2 revision + 4 replay = 362 calls. No further exploration within this follow-up. All acquisition/validation/revision/inference/switch/verification costs and failures preserved. Report original adapter training cost separately from its zero additional training in this follow-up. No projected break-even if the adapter underperforms examples or selected lesson; only report measured costs with performance. Human labor, energy, dollars and production caching remain unmetered.

Stopping decision: complete the fixed follow-up regardless of winner; characterize remaining limitations, update README with current evidence and scope, and link a local addendum. Do not silently overwrite the initial report or claim the general study question is fully settled. No external publication or larger campaign.
